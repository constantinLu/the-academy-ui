import 'package:flutter/material.dart';
import 'package:the_academy/constants/app_color.dart';

class CampusView extends StatelessWidget {
  const CampusView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Campus.' + construction),
    );
  }
}
